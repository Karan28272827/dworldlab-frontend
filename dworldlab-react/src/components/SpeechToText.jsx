import React from "react";

const SpeechToText = () => (
  <div className="tool-output">The Web Speech API for recognition is still experimental and varies by browser. Placeholder for now.</div>
);

export default SpeechToText; 