import React from "react";

const ImageCropper = () => (
  <div className="tool-output">Image cropping requires a complex canvas interface. This is a placeholder.</div>
);

export default ImageCropper; 