import React from "react";

const AudioTrimmer = () => (
  <div className="tool-output">Audio trimming requires the Web Audio API and buffer manipulation. This is a placeholder for a complex feature.</div>
);

export default AudioTrimmer; 